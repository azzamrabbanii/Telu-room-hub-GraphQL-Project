type Room {
  id: ID!
  name: String!
  capacity: Int!
  facility: [String!]!
  isAvailable: Boolean!
}

type Query {
  getAllRooms: [Room!]!
}

type Mutation {
  addRoom(id: ID!, name: String!, capacity: Int!, facility: [String!]!): Room!

  deleteRoom(id: ID!): String!
}
